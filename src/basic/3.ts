let mixedType: string | number;

mixedType = 'string'; 
mixedType = 10;

let state: 'enable' | 'disable';

state = 'enable';
console.log("State:", state);

state = 'disable';
console.log("State:", state);

export {};
